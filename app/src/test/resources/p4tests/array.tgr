let
        type int_arr := array [100] of int;
        var a : int_arr;
        var i : int;
in
        a[3] := 3;
        a[4] := 13;
        a[2] := 2;
        for i := 3 to 6 do
                printi(a[i] * 2);
        enddo;
end
