let
	type ArrayFloat := array [5] of float;
	var A : ArrayFloat;
	var i : int := 0;
in
    for i := 0 to 5 do
        A[i] := 2.5;
    enddo;
	A[4] := 1.5;
	for i := 0 to 5 do
		printf(A[i]);
	enddo;
end
